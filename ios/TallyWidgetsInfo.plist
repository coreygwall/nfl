<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Deliberately a sibling of TallyWidgets/ rather than a file inside it. The target's sources are
     a synchronised folder, so anything in that folder is also copied in as a resource — and an
     Info.plist that is both processed and copied is "Multiple commands produce", which is a build
     failure rather than a warning. Out here there is only ever one command. -->
<plist version="1.0">
<dict>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.widgetkit-extension</string>
	</dict>
</dict>
</plist>
